# Copy to .env and adjust if needed. No real secrets belong in this file or
# in .env — see CLAUDE.md ("no secrets in logs or code").

# Matches docker-compose.yml's postgres service.
DATABASE_URL=postgresql://hoosradar:hoosradar_dev@localhost:5432/hoosradar

# minimal | pretty
LOG_LEVEL=info

# Worker scheduler tick interval, seconds. See docs/adr/0004-job-mechanism.md.
WORKER_TICK_SECONDS=60
